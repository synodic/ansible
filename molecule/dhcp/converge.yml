---
- name: Converge
  hosts: instance
  gather_facts: false
  tasks:
    - name: Replace this task with one that validates your content
      ansible.builtin.include_role:
        name: synodic.infrastructure.dhcp
